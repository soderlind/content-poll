{
	"name": "persoderlind/content-vote",
	"description": "Adds a configurable vote block (2–6 options) for collecting anonymous sentiment on content pages.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": "^8.1"
	},
	"autoload": {
		"psr-4": {
			"ContentVote\\": "src/php/"
		}
	},
	"require-dev": {
		"phpunit/phpunit": "^10.5",
		"squizlabs/php_codesniffer": "^3.9"
	},
	"scripts": {
		"test": "phpunit",
		"lint": "phpcs --standard=WordPress --ignore=vendor src/php"
	}
}